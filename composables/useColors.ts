export enum Colors {
  NEUTRAL = "neutral",
  PRIMARY = "primary",
  SUCCESS = "success",
  DANGER = "danger",
  WARNING = "warning",
  BLACK = "black",
}

export const useColors = () => {
  return Colors;
};
